// @ts-check
import { defineConfig } from 'astro/config'

/**
 * A static site, which is the whole point of it.
 *
 * The tools this site is for hold their state in the browser and send nothing anywhere, and a
 * server would be a thing to explain away rather than a thing anyone needs.
 */
export default defineConfig({})
